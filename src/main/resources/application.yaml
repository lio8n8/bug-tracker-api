spring:
  profiles:
    active: dev
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/bugtracker?createDatabaseIfNotExist=true
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  liquibase:
    user: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/bugtracker
    change-log: classpath:db/changelog/db.changelog-master.xml
server:
  port: 8080
  servlet:
    context-path: /api
